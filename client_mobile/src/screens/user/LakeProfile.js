import { View, Text, TextInput, TouchableOpacity, Image, FlatList, StyleSheet, ActivityIndicator } from 'react-native';

const LakeProfile = () => {
  return (
    <View>
      <Text>LakeProfile</Text>
    </View>
  );
};

export default LakeProfile;
